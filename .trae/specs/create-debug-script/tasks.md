# Tasks
- [ ] Task 1: Create `tests/debug_doc_structure.py`.
  - [ ] Initialize `Archivist`.
  - [ ] Load the contract document.
  - [ ] Extract chunks and generate the structure string.
  - [ ] Print the result for manual inspection.
  - [ ] Add basic checks for summary length (<100 words) and tree depth (no "X.X.X").
